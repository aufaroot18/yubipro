import styled from "styled-components";

const Main = styled.main`
  max-width: 650px;
  margin: 0 auto;
  background-color: #fff;
  height: 100%;
  overflow: hidden;
  padding-left: 3rem;
  padding-right: 3rem;
`;

export default Main;
